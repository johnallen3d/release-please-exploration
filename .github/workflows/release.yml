name: Release

on:
  pull_request:
    branches:
      - main
    types: [closed]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - name: Dump GitHub context
      run: echo '${{ toJSON(github) }}'
    - name: Dump env
      run: env
    - name: Release Mock
      if: github.event.pull_request.merged == true && github.head_ref == 'release-please--branches--main'
      run: echo 'release would run now!'

